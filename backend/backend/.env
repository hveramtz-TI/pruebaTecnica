# Django Settings
SECRET_KEY=django-insecure-9j_4^tkb97ihm$0^a%3quaw%d)m#(r6-iue4anu&mws&^t-71v
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (SQLite por defecto)
DATABASE_URL=sqlite:///db.sqlite3

# Redis y Celery
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Para desarrollo sin Redis (ejecutar tareas síncronamente)
# CELERY_TASK_ALWAYS_EAGER=True

# Para usar Redis (ejecutar tareas asíncronamente)
CELERY_TASK_ALWAYS_EAGER=False

# Email Settings (desarrollo - console)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=noreply@sorteo-san-valentin.com

# Email Settings (producción - SMTP)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=tu-email@gmail.com
# EMAIL_HOST_PASSWORD=tu-app-password

# CORS Settings (desarrollo)
CORS_ALLOW_ALL_ORIGINS=True
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# JWT Settings
JWT_SECRET_KEY=tu-jwt-secret-key-aqui
JWT_ACCESS_TOKEN_LIFETIME=20
JWT_REFRESH_TOKEN_LIFETIME=7